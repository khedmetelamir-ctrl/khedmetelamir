# خدمة الأمير — إعدادات ما بعد التقوية

## 1) قاعدة البيانات
شغّل `SECURITY_HARDENING.sql` مرة واحدة في Supabase SQL Editor. الحزمة تقفل الوصول حسب الفصل، تحمي الملاحظات والحضور والزيارات والإشعارات، تمنع إزالة آخر مدير، تنظف السجلات التابعة عند حذف مخدوم، وتجهز Storage للصور.

## 2) البريد
موثوقية رسائل تأكيد الحساب وإعادة كلمة المرور تعتمد على SMTP مخصص في Supabase. لا توجد بيانات SMTP محفوظة في المستودع، لذلك لم يتم وضع أسرار أو بيانات وهمية في الكود.

## 3) Push الحقيقي عند إغلاق التطبيق
تمت إضافة معالجة Push داخل Service Worker، لكن الإرسال الحقيقي يحتاج اشتراك Web Push ومفتاح VAPID + Edge Function/مزود إرسال. لا يتم وضع مفاتيح سرية في الواجهة.

## 4) الاختبار
تمت إضافة GitHub Action لفحص JavaScript المضمّن في `index.html` عند كل push/PR.
